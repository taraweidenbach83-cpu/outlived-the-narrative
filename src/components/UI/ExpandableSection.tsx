'use client';\n\nimport { useState } from 'react';\nimport { ChevronDown } from 'lucide-react';\n\ninterface ExpandableSectionProps {\n  title: string;\n  subtitle?: string;\n  children: React.ReactNode;\n  defaultOpen?: boolean;\n  id?: string;\n  intensity?: 'light' | 'medium' | 'heavy';\n}\n\nexport function ExpandableSection({\n  title,\n  subtitle,\n  children,\n  defaultOpen = false,\n  id,\n  intensity = 'light',\n}: ExpandableSectionProps) {\n  const [isOpen, setIsOpen] = useState(defaultOpen);\n\n  const intensityColors = {\n    light: 'border-accent-teal/30 bg-accent-teal/5',\n    medium: 'border-accent-purple/40 bg-accent-purple/10',\n    heavy: 'border-accent-deep/50 bg-accent-deep/10',\n  };\n\n  return (\n    <div\n      id={id}\n      className={`border-l-4 ${intensityColors[intensity]} p-6 rounded-lg transition-all duration-300 fade-in`}\n    >\n      <button\n        onClick={() => setIsOpen(!isOpen)}\n        className=\"flex items-center justify-between w-full text-left group mb-2\"\n        aria-expanded={isOpen}\n      >\n        <div>\n          <h3 className=\"text-2xl font-accent font-bold text-accent-teal group-hover:text-text-primary transition-colors\">\n            {title}\n          </h3>\n          {subtitle && (\n            <p className=\"text-sm text-text-secondary mt-1\">{subtitle}</p>\n          )}\n        </div>\n        <ChevronDown\n          className={`w-6 h-6 text-accent-teal transition-transform duration-300 flex-shrink-0 ml-4 ${\n            isOpen ? 'rotate-180' : ''\n          }`}\n        />\n      </button>\n\n      {isOpen && (\n        <div className=\"mt-6 space-y-6 text-text-secondary animate-in fade-in duration-300\">\n          {children}\n        </div>\n      )}\n    </div>\n  );\n}\n